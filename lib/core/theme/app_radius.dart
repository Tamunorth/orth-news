/// Shared corner radii so the search field, category chips and segmented
/// controls all read as the same family.
abstract final class AppRadius {
  static const field = 12.0;
  static const segment = 8.0;
  static const card = 16.0;
}
